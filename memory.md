# Protocol Phase 0 — Memory (redirect)

Project state lives in `docs/`, and `CLAUDE.md` lists the files and the session protocol.

**Read `CLAUDE.md`, not this file.**

This file deliberately carries no list of its own: a second copy drifts from the first.
